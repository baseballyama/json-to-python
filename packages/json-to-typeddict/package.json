{
  "name": "json-to-typeddict",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.mjs",
  "types": "./dist/index.d.mts",
  "bin": "./src/cli.mjs",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./src/index.mjs"
    }
  },
  "scripts": {
    "format:fix": "prettier --cache --write \"./src/**/*.{js,ts,json}\"",
    "format:check": "prettier --cache --check \"./src/**/*.{js,ts,json}\"",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "publish:check": "publint",
    "test:all": "pnpm format:check && pnpm test && publish:check",
    "typegen": "rm -rf dist && tsc -P tsconfig.json --emitDeclarationOnly"
  },
  "dependencies": {
    "publint": "^0.2.0"
  }
}
